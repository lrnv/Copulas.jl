# Published results

This directory holds reviewed snapshot results produced by the full
cross-language workflow. Raw results from every run are retained as GitHub
Actions artifacts; snapshots should only be committed after checking the
correctness report and runner metadata.
